import 'package:flutter/material.dart';

class MiktarPage extends StatelessWidget {
  const MiktarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      drawer: AppBar(
        title: Text('BOTAŞ Müşteri Portalı'),
        backgroundColor: Color.fromARGB(255, 210, 20, 26),
        
      ),
    );
  }
}
